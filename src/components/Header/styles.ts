import styled from "styled-components";

export const Container = styled.header`
  background-color: #ffff;

  img {
    position: relative;
    left: 50%;
    top: 50%;
    align-items: center;
    margin-top: 10px;
  }
`;
